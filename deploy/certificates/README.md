# Development certificates

Do not commit certificates or private keys. Generate local HTTPS material outside Git and mount it at runtime. Production termination and rotation belong to the deployment platform.
